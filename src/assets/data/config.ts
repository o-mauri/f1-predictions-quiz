import { Race } from '../../types/main-types';
import { Races } from '../../types/races';

/** Set this to the most recently completed race to update the "Correct as of" badge. */
export const currentRace: Race = Races.austria;
